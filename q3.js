// Area of circle
const pi = 3.14;
let radius = prompt("Enter the radius: ");

let calArea = (radius) => pi * radius * radius;
console.log(`Area of circle is ${pi} * ${radius} * ${radius}: ${calArea(radius)}`);

